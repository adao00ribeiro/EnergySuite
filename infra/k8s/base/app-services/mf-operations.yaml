apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-operations
  namespace: energysuite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mf-operations
  template:
    metadata:
      labels:
        app: mf-operations
    spec:
      containers:
        - name: mf-operations
          image: energysuite/mf-operations:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: mf-operations
  namespace: energysuite
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: mf-operations
