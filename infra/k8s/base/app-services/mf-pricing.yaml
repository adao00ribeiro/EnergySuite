apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-pricing
  namespace: energysuite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mf-pricing
  template:
    metadata:
      labels:
        app: mf-pricing
    spec:
      containers:
        - name: mf-pricing
          image: energysuite/mf-pricing:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: mf-pricing
  namespace: energysuite
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: mf-pricing
